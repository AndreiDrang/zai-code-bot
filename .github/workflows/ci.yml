name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: poc/package-lock.json
      - name: Install dependencies
        run: npm ci
        working-directory: poc
      - name: Run tests with coverage
        run: npm test
        working-directory: poc

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./poc/coverage/lcov.info
          fail_ci_if_error: false

  workers-dry-run:
    name: Workers dry run
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: poc/package-lock.json
      - name: Install dependencies
        run: npm ci
        working-directory: poc
      - name: Install main Worker tooling
        run: npm install --no-package-lock --ignore-scripts
        working-directory: poc/workers/zai-main-worker
      - name: Install heavy Worker tooling
        run: npm install --no-package-lock --ignore-scripts
        working-directory: poc/workers/zai-heavy-worker
      - name: Build
        run: npm run deploy:main:dry-run && npm run deploy:heavy:dry-run
        working-directory: poc

  security-audit:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: poc/package-lock.json
      - name: Install dependencies
        run: npm ci
        working-directory: poc
      - name: Run security audit
        run: npm audit --audit-level=moderate
        working-directory: poc
