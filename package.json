{
  "name": "zai-code-bot",
  "version": "0.0.1",
  "description": "GitHub Action for PR auto-review and /zai assistant commands powered by Z.ai",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --license licenses.txt",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "yaml": "^2.8.2"
  }
}
